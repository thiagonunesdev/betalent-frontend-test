import borderRadius from './borderRadius.json'
import colors from './colors.json'
import fonts from './fonts.json'
import shadows from './shadows.json'
import spaces from './spaces.json'

export const Theme = {
  borderRadius,
  colors,
  fonts,
  shadows,
  spaces
}
